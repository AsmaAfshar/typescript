let famouspersons:string = "Albert Einstine";
let famousquote:string ='"student is not a container you have to fill but a torch you have to light up."'

let message:string =`${famouspersons} once said, ${famousquote}`;
console.log(message);
